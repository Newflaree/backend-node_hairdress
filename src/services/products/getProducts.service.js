// Models

const getProductsService = async ( query, from, limit ) => {
  try {

  } catch ( err ) {
    console.log( `${ '[SERVICE.GET-PRODUCTS]'.red }: Error Detail - ${ err }` );
  }}

module.exports = getProductsService;
