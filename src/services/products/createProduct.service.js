// Models

const createProductService = async ( name = '', uid = '' ) => {
  try {

  } catch ( err ) {
    console.log( `${ '[SERVICE.CREATE-PRODUCT]'.red }: Error Detail - ${ err }` );
  }
}

module.exports = createProductService;
