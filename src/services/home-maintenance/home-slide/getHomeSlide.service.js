// Models
const { HomeSlide } = require( '../../../models' );

const getHomeSlideService = async ( req = request, res = response ) => {
  try {

  } catch ( err ) {
    console.log( `${ '[SERVICE.GET-HOME-SLIDE]'.red }: Error Detail - ${ err }` );
  }
}

module.exports = getHomeSlideService;
