// Models
const { HomeSlide } = require( '../../../models' );

const deleteHomeSlideByIdService = async ( req = request, res = response ) => {
  try {

  } catch ( err ) {
    console.log( `${ '[SERVICE.DELETE-HOME-SLIDE-BY-ID]'.red }: Error Detail - ${ err }` );
  }
}

module.exports = deleteHomeSlideByIdService;
