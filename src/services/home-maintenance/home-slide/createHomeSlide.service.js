// Models
const { HomeSlide } = require( '../../../models' );

const createHomeSlideService = async ( req = request, res = response ) => {
  try {

  } catch ( err ) {
    console.log( `${ '[SERVICE.CREATE-HOME-SLIDE]'.red }: Error Detail - ${ err }` );
  }
}

module.exports = createHomeSlideService;
